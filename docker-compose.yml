services:
  cardanalyser:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BASE_IMAGE: ${BASE_IMAGE:-python:3.11-slim}
        INSTALL_DEPS: ${INSTALL_DEPS:-1}
    container_name: cardanalyser
    ports:
      - "8047:8047"
    restart: unless-stopped
